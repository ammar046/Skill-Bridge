# Frontend (Vite) — copy to .env.local for local overrides
# Production: set VITE_API_BASE_URL in Vercel to your Render API URL (no trailing slash).
# Example: VITE_API_BASE_URL=https://skill-bridge-api.onrender.com
VITE_API_BASE_URL=http://localhost:8000
